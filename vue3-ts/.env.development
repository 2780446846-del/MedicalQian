# 本机开发：后端和前端在同一台电脑时用 localhost
# 真机/局域网访问：填运行后端的电脑 IP（ipconfig 里「WLAN」或「以太网」的 IPv4，注意是 192.168.x.x 不是 192.108）
# 不要用 VMware 的 192.168.91.1，手机和电脑需在同一 WiFi
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000
